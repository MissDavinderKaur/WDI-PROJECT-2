<h1><%= user.name %></h1>
<h5> <b> Email: </b> <%= user.email %></h5>
<h5> <b> Phone: </b> <%= user.phone %></h5>
<br>
<h2>My To-Do List</h2>
<br>
<a href="/tasks/new"> <button class="btn btn-primary">  Create New Task </a> </button>
<br>
<br>

<div class="row">
<div class="col-xs-6 col-md-3">
  <p class="list-group-item active"> Work </p>
  <% tasksO.forEach(task => {
  if (task.category === "Work") {%>
    <a href="/tasks/<%= task._id %>" class="list-group-item" ><%= task.description %> <small> (due by <%= task.endDate.toDateString() %> )</small> </a>
  <% } }) %>
  </div>
  <div class="col-xs-6 col-md-3">
    <p class="list-group-item active"> Home </p>
    <% tasksO.forEach(task => {
    if (task.category === "Home") {%>
      <a href="/tasks/<%= task._id %>" class="list-group-item" ><%= task.description %> <small> (due by <%= task.endDate.toDateString() %> )</small></a>
    <% } }) %>
  </div>
  <div class="col-xs-6 col-md-3">
    <p class="list-group-item active"> Social </p>
    <% tasksO.forEach(task => {
    if (task.category === "Social") {%>
      <a href="/tasks/<%= task._id %>" class="list-group-item" ><%= task.description %><small> (due by <%= task.endDate.toDateString() %> )</small></a>
    <% } }) %>
  </div>
  <div class="col-xs-6 col-md-3">
    <p class="list-group-item active"> Personal </p>
    <% tasksO.forEach(task => {
    if (task.category === "Personal") {%>
      <a href="/tasks/<%= task._id %>" class="list-group-item" ><%= task.description %><small> (due by <%= task.endDate.toDateString() %> )</small></a>
    <% } }) %>
  </div>
</div>
<br>

<h2>Tasks Assigned To Me </h2>
<div class="row">
<div class="col-xs-6 col-md-3">
  <p class="list-group-item active"> Work </p>
  <% tasksA.forEach(task => {
  if (task.category === "Work") {%>
    <a href="/tasks/<%= task._id %>" class="list-group-item" ><%= task.description %><small> (due by <%= task.endDate.toDateString() %> )</small></a>
  <% } }) %>
  </div>
  <div class="col-xs-6 col-md-3">
    <p class="list-group-item active"> Home </p>
    <% tasksA.forEach(task => {
    if (task.category === "Home") {%>
      <a href="/tasks/<%= task._id %>" class="list-group-item" ><%= task.description %><small> (due by <%= task.endDate.toDateString() %> )</small></a>
    <% } }) %>
  </div>
  <div class="col-xs-6 col-md-3">
    <p class="list-group-item active"> Social </p>
    <% tasksA.forEach(task => {
    if (task.category === "Social") {%>
      <a href="/tasks/<%= task._id %>" class="list-group-item" ><%= task.description %><small> (due by <%= task.endDate.toDateString() %> )</small></a>
    <% } }) %>
  </div>
  <div class="col-xs-6 col-md-3">
    <p class="list-group-item active"> Personal </p>
    <% tasksA.forEach(task => {
    if (task.category === "Personal") {%>
      <a href="/tasks/<%= task._id %>" class="list-group-item" ><%= task.description %><small> (due by <%= task.endDate.toDateString() %> )</small></a>
    <% } }) %>
  </div>
</div>

<br>

<h2>My Deleted Tasks </h2>
<ul>
  <% tasksD.forEach(task => {%>
    <li> <%= task.description %> </li>
  <% }); %>
</ul>
